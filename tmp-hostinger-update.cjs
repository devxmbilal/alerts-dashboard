const { Client } = require('ssh2'); const conn = new Client(); conn.on('ready', () => { console.log('Connected to Hostinger'); conn.exec('cd /var/www/alerts-dashboard && git pull origin main && npm run build && pm2 restart alerts-dashboard', (err, stream) => { if (err) throw err; stream.on('close', () => { console.log('Update complete'); conn.end(); }).on('data', (d) => process.stdout.write(d)).stderr.on('data', (d) => process.stderr.write(d)); }); }).on('error', (err) => console.log('SSH Error:', err)).connect({ host: '157.173.221.83', port: 22, username: 'root', password: 'Arham@810000', readyTimeout: 20000 });
